#ifndef _PICTURE_H_
#define _PICTURE_H_

#include <string>
#include <iostream>

class Picture
{
 public:
    Picture();
    Picture(const std::string& file);
    ~Picture();
    Picture &operator=(Picture &);
    
    bool getPictureFromFile(const std::string& file);
    std::string   data; 
};

#endif

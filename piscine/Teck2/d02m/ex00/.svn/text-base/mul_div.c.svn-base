void add_mul_4param(int first, int second, int *add, int *mul)
{
  *add = first+second;
  *mul = first*second;
}

void add_mul_2param(int *first, int *second)
{
  int a = *first;
  int b = *second;
  int add;
  int mul;

  add = a+b;
  mul = a*b;

  *first = add;
  *second = mul;
}
